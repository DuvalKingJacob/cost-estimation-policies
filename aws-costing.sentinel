import "tfrun"
import "decimal"

# Use a conditional to handle null
monthly_cost = tfrun.cost_estimate.proposed_monthly_cost is null ? 0.0 : tfrun.cost_estimate.proposed_monthly_cost

print("Proposed Monthly Cost: " + string(monthly_cost))

main = rule {
    decimal.new(monthly_cost).less_than(10)
}
